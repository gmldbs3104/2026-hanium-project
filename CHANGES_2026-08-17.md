# 2026-08-16 ~ 17 변경 내역 · 남은 문제 (인수인계용)

> 기준점: **`ab9de5a`**(2026-08-11, GitHub에서 당겨온 시점) → **현재**
> 이 기간에 `DATA_FLOW.md`의 불일치를 코드로 전수 검증하고 미해결분을 고쳤다.
> 값 흐름 전체 대조표는 [DATA_FLOW.md](DATA_FLOW.md), 경위는 [ai/DEVLOG.md](ai/DEVLOG.md) 18~23막,
> 남은 일의 단일 출처는 [ai/STATUS.md](ai/STATUS.md).

## 한눈에

| | |
|---|---|
| 커밋 | 7개 (전부 로컬, 푸시 전) |
| 코드 파일 | AI 4 · 백엔드 8 · 프론트 8 |
| DB | 마이그레이션 1개(`b3f1c27a9d40`) — 컬럼 4개 추가 |
| 테스트 | AI 34개 통과 · 프론트 **36통과 1실패**(작업 전 33·3) |
| 해결 | DATA_FLOW 필수 7건·연결 4건·캔버스 7건 **전부** |
| **남은 문제** | **6건** (아래 §3) |

⚠️ **팀원이 각자 해야 할 것 2가지** — §4 참고. 안 하면 서버가 안 뜨거나 DB가 안 맞는다.

---

## 1. 고친 것

### 1-1. 못 잰 지표가 만점으로 나가던 문제 `8a660c4`

한 줄만 쓰면 행간을, 한 행에 3글자 미만이면 기울기를 **잴 수 없다.** 그런데 AI가 그럴 때
스스로 `100.0`(만점)으로 덮어 내보내고 있었다. 결과적으로 **재지도 않은 지표로 사용자를
칭찬**했고, 그 가짜 만점이 대시보드 평균까지 오염시켰다.

원인은 문서에 적힌 것보다 한 단계 앞에 있었다 — "백엔드가 사유를 버린다"가 아니라
**AI가 먼저 만점으로 덮고** 있었다.

| 파트 | 변경 |
|---|---|
| AI | `handwriting_analyzer.py` — 세 점수를 `Optional`로, `.get("score", 100.0)` → `.get("score")` |
| 백엔드 | `image.py` — **5지표를 전부 `metrics`에서 `_metric_score()`로** 읽도록 통일 |
| 백엔드 | `image.py` — 피드백 문구가 `None`이면 아무 문구도 만들지 않음 |
| 백엔드 | `schemas/image.py` — 점수 3필드 `Optional[int]` |
| 백엔드 | `dashboard_service.py` — `or 0` 제거, `None` 항목을 집계에서 뺌 |

**함께 고친 것**: 탐지 0개여도 `/analyze`가 통과해 **종합 100점·"우수"**가 나갔다(빈 종이에
"완벽합니다"). 이제 **400**으로 *"사진에서 글자를 찾지 못했습니다"*를 준다.

> 검증 — 안 잰 "기울기 일관성"이 **26.7점(최하위 취약항목) → 80.0점**(측정된 세션만)으로 바뀐다.

### 1-2. 채점 계수가 두 벌이던 문제 `e874828`

08-11에 캔버스 채점을 AI로 넘겼는데 **대시보드만 옛 경로**가 남아, 저장된 편차를 백엔드
설정 계수로 다시 계산했다. 계수가 서로 달라 **같은 글씨인데 화면마다 점수가 어긋났다.**

| 감점 | AI(결과 화면) | 백엔드(분석 화면) |
|---|---|---|
| 크기 | 편차 × **0.8**, 최대 50 | 편차 × **0.5**, 최대 30 |
| 자간 | 편차 × **0.3**, 최대 30 | 편차 × **0.5**, 최대 30 |
| 획순 | 오류 × **15**, 최대 40 | 오류 × **10**, 상한 없음 |

실측 예: 크기 편차 −40% 글자가 **AI 68점 / 백엔드 80점 — 12점 차이.**

- AI에 `canvas_item_scores()` 신설 — 항목 점수의 **단일 출처**. 종합 점수도 여기서 유도.
- 백엔드는 `ai_adapters`를 통해 그 함수를 **호출만** 한다.
- `config.py`에서 계수 8개 삭제(캔버스 5 + 아무 데서도 안 읽히던 이미지 가중치 3).
- 획순은 목표 글자를 모르면 `None` — 종전엔 `error_count=0`으로 보고 **만점**을 줬다(§4-1과 동형).

### 1-3. 사라지던 값을 DB에 쌓기 시작 `08019d3`

계산은 되는데 저장이 안 돼 화면 한 번 스치고 사라지던 값들. **소급이 안 되므로** 화면 노출
여부와 무관하게 컬럼부터 만들었다.

마이그레이션 **`b3f1c27a9d40`** — 컬럼 4개, 전부 nullable:

| 테이블 | 추가 |
|---|---|
| `image_analysis_results` | `spacing_uniformity_score`, `line_spacing_uniformity_score` |
| `canvas_analysis_results` | `speed_profile`, `correction_flags` |

**프론트 수정 0줄로** 분석 화면에 자간·행간이 등장한다 — 취약 항목 목록이 항목 이름을 미리
정해두지 않고 서버가 준 딕셔너리를 그대로 그리는 구조라서다.

> 검증(실제 개발 DB) — 기존 데이터 보존(캔버스 22·이미지 2·유저 1), 취약 항목에
> **"자간 균등성 55.0"이 2위로 등장**. 종전엔 후보로도 없던 항목이다.

⚠️ **필압(`pressure_profile`)은 일부러 뺐다** — 프론트가 `canvas_input_screen.dart:99`와
`sentence_practice_screen.dart:83`에서 `pressure: 1.0`을 하드코딩해 보낸다. 전부 같은 값이라
쌓아도 볼 게 없다.

### 1-4. 연한 글씨 사진에 비침 안내 `9d086b1`

연필처럼 연하게 쓴 사진은 비침(뒷면 글씨)을 지우면 진짜 획까지 지워져서, 서버가 비침을
남기는 쪽을 택한다. 그 결과 비친 자국이 글자로 잡혀 점수에 섞이는데 **사용자는 이유를 모른다.**

백엔드는 08-09부터 `preservation_mode`를 보내고 있었고 프론트만 안 읽었다. 배관 3곳을 잇고
안내 위젯(`preservation_notice.dart`)을 오버레이 바로 아래에 붙였다. 백엔드·AI 무변경.

> 탐지 로직은 만들지 않는다(팀 결정) — 비침과 연한 진짜 획은 진하기로 구분이 안 되고
> (`image_preprocessor.py:30` 실측), 걸러내려면 확률값이 필요한데 파이프라인에 오지 않는다.

### 1-5. 오버레이 배경을 회전만 한 컬러로 `6eb6e38`

사용자에게 **이진화된 흑백**이 보이고 있었다. 08-06 기록은 *"A(이진) vs B(회전만 한 컬러)
팀 논의 대기"*였는데, 08-09에 프론트가 좌표 어긋남을 고치며 이진본을 붙였고 그것이
"확정"으로 잘못 기록됐다. **B로 되돌렸다.**

좌표를 바꾸는 단계는 회전·리사이즈뿐이라(1~5단계는 크기를 안 바꾼다) 원본 컬러에 같은
회전·같은 크기만 적용하면 좌표가 정확히 일치한다.

| 파트 | 변경 |
|---|---|
| AI | `PreprocessResult.display_image` 추가, `_rotate_image(for_display=True)`(흰 여백·`INTER_LINEAR`) |
| 백엔드 | `display_image_base64`(JPEG q82, 장축 1280) 응답 추가 |
| 프론트 | 배경을 컬러본으로. 없으면 이진본 → 원본 폴백 |

**채점 경로(`binary_image`)는 한 줄도 안 건드렸다 — 점수·F1 불변.**
이진본은 개발·디버그용으로 남긴다(AI가 실제로 본 것이라 획 끊김 확인에 유용).

> 검증 — 5장에서 `binary`와 `display` 크기 **완전 일치**(deskew +1.03°인 test8 포함),
> 종횡비 일치, 전송 크기 **40~344KB**(장축 1280 기준, 사진 크기에 따라).

**함께**: 이미지 모드 오버레이에서 탐지 신뢰도로 박스 색을 가르던 분기를 제거했다.
그 값이 항상 `0.5` 상수라 조건에 늘 걸려 **모든 박스가 주황**이었고, 색 구분 자체가
`requirement.md`에 없던 동작이었다.

### 1-6. 문서 정리 `1a4a04a` `c93cccc`

- **AI 문서 원본을 통합본으로 일원화.** 파인튜닝은 성공이 불확실한 실험이라 별도 클론
  (`2026-hanium-project`)에서만 돌리고, 여기가 배포 코드·문서의 단일 출처다.
  두 클론의 `ai/CLAUDE.md` 최상단에 **"여기가 어디인가"** 배너를 넣었다.
- **루트 `CLAUDE.md`의 "Current Implementation Status"가 2026-04 시점 그대로**여서 다섯 곳이
  사실과 반대였다(CRAFT를 "TODO", 전처리를 "Otsu", 삭제된 `canvas_analysis.py`를 구조도에
  싣는 등). 실제 코드 기준으로 전면 교체했다. **auto-load되는 첫 문서라 영향이 컸다.**
- 🐛 **"요구사항 미충족"이라 적었다가 철회했다** — SFR-004I의 *"신뢰도 0.5 미만 필터링"*을
  못 하고 있다고 적었는데, 그 필터링은 **CRAFT 디코딩이 이미 한다**(`text_threshold=0.7`·
  `low_text=0.4`, 평가셋 측정으로 정한 값). 응답의 `confidence`는 게이트가 아니라 **보고용**인데
  그걸 게이트로 착각했다. 지금 와서 0.5 필터를 얹으면 **이중 필터링이고 F1 기준선(0.891)이
  무효**가 된다. 같은 오해가 반복되지 않도록 값이 만들어지는 자리·소비되는 자리 양쪽에 주석을 남겼다.

### 1-7. 프론트 테스트 2건 복구 *(미커밋)*

**2026-08-02 백엔드 연동 커밋(`f11fa78`)에서 3건이 깨진 채 2주간 방치**돼 있었다. 그날
`useMockApi`가 `true → false`로 바뀌고 로그인 화면이 Firebase 기반으로 재작성됐는데,
테스트는 07-25가 마지막 수정이라 그 전 세계를 보고 있었다.

- ✅ **저장 큐** — mock 성공 경로를 되살릴 수 없어 **"서버 없을 때 큐 유지"**(REQ-009-5)
  검증으로 바꿨다. 재시도 큐를 둔 본래 목적에 오히려 가깝다.
- ✅ **로그인 화면 표시** — 문구를 현재 화면(`Google 계정으로 시작하기`)에 맞췄다.
- ⛔ **모드 분기** — 테스트는 다 고쳤으나 **제품 결함에 막혀 여전히 실패**(§3-1).

**실제 로그인은 자동화하지 않는다(결정).** 앱을 열 때마다 사람이 반드시 거치는 관문이라
깨지면 누구도 못 놓친다. 반면 자동화 비용(테스트 계정·Firebase 설정)이 크고 외부 서비스라
오탐이 잦다. ⚠️ **"빠뜨린 것"이 아니라 "사람이 하기로 정한 것"**이다.

---

## 2. 검증 결과 (2026-08-17 실행)

| 항목 | 결과 |
|---|---|
| AI 단위 테스트 | ✅ **34개 통과** |
| 프론트 테스트 | 🟡 **36통과 · 1실패** (§3-1) |
| 프론트 정적분석 | ✅ 컴파일 오류 0 (경고 5건은 이번에 안 건드린 기존 코드) |
| 백엔드 전 모듈 import | ✅ 라우트 17개 등록 |
| 마이그레이션 | ✅ `8cc78a9d44f7` → `b3f1c27a9d40`, 기존 데이터 보존 |
| E2E `/image/preprocess` | ✅ 200 · 3.5s · 표시용 컬러 137KB · 이진 91KB |
| E2E `/image/{id}/detect` | ✅ 200 · 15.0s · 18글자 |
| E2E 빈 이미지 | ✅ 품질 22 → 재촬영 필요, 탐지 **0글자** |
| AI→백엔드 전체 경로 | ✅ 5지표 산출·좌표계 일치·보존모드 판정 정상 |

---

## 3. 남은 문제 (고쳐야 할 것)

### 3-1. ⛔ 통계 전송 실패가 화면 이동을 막는다 `프론트`

```dart
// home_screen._goPractice
AnalyticsService.logModeSelected(mode.name);   // ← 여기서 예외가 나면
context.go(route);                              //   이 줄에 도달 못 한다
```

`FirebaseAnalytics.instance`는 초기화 전이면 **동기적으로** 던지므로 `await`을 안 붙여도
예외가 탭 핸들러까지 올라간다. 즉 **연습 카드를 눌러도 화면이 안 넘어간다.** 사용자는
"버튼이 안 먹는다"고 느끼는데 원인은 통계다 — **우선순위가 뒤집혀 있다.**

- **지금은 재현 안 됨** — `main.dart`가 Firebase를 초기화하므로. 초기화 실패·설정 누락 시 드러난다.
- **위젯 테스트는 항상 이 경로를 탄다** → 프론트 테스트 1건 실패의 원인.
- **고치는 법**: 그 호출을 `try/catch`로 감싼다(통계는 부가 기능이므로 실패해도 흐름을 막으면 안 된다).
  한 번 고쳐서 37개 전부 초록인 것까지 확인했으나, **제품 동작 변경이라 담당 파트 판단으로 넘긴다.**
  재현 조건·수정 방법은 `analytics_service.dart:_logEvent` 주석에 남겨뒀다.

### 3-2. ⛔ 프론트가 이미 호출하는데 백엔드에 없는 API 3종 `백엔드`

**지금 화면에서 실패한다.**

| 프론트가 부르는 것 | 증상 |
|---|---|
| `DELETE /api/v1/user/history` (설정 → 데이터 초기화) | 버튼을 누르면 실패 |
| 프로필 수정 API | 닉네임·사진이 기기에만 저장 |
| 계정 삭제 | 스펙 자체가 없음 (백엔드에 DELETE/PATCH 라우트 0개) |

### 3-3. 🟡 "미측정" 사유가 응답에 없다 `AI + 백엔드`

점수는 `None`으로 정확히 가는데 **왜 못 쟀는지**(`{"skipped": "행이 부족해 행간 평가 생략"}`)는
안 실린다. 화면에 "미측정" 옆에 이유를 보여주려면 필요하다. §3-4와 묶인다.

### 3-4. 🤝 세부 점수를 화면에 보여줄지 — 팀 결정 대기 `프론트`

AI는 5지표로 채점하는데 사용자는 **종합 점수 하나만** 본다. 분석 화면(누적)에는 항목별
점수가 이미 나오지만, 결과 화면(그때그때)에는 안 나온다. **저장은 끝났으니 결정만 나면
붙이기만** 하면 된다.

### 3-5. 🟡 자모 단독(ㄱ·ㅏ) 획순 채점 불가 `AI`

획순을 채점하려면 글자를 자모로 쪼개야 하는데, 이미 자모면 더 쪼갤 게 없어 **획순 채점을
건너뛴다**(`stroke_order_result: null`). 연습 화면에 **자음 쓰기·모음 쓰기 탭**이 있어
`ㄱ ㄴ ㄷ ㄹ ㅁ` / `ㅏ ㅑ ㅓ ㅕ ㅗ` 10글자가 여기 걸린다 — **연습 세트의 3분의 2**다.
기초 연습일수록 획순이 중요한데 거기서 빠진다.

### 3-6. 🟡 낡은 문서 `AI`

가중치 표기 `3:2:1`이 `handwriting_evaluation.md` 3곳·`IMPLEMENTATION_PLAN.md`에 남아 있다
(실제 코드는 `3:3:3:2:2`). `FRONTEND_CHANGES.md`·`BACKEND_CHANGES.md`의 "캔버스는 백엔드
미수정" 서술도 이미 거짓이다.

### 보류로 정한 것

- **필압을 진짜 값으로** — 프론트가 `1.0` 하드코딩 중
- **추천 연습 예문** — `dashboard_service.py:237`이 항상 빈 배열
- **글자별 명료도 노출** — 사용자에게 보여줄 필요 없음으로 결정, AI 내부 채점 게이트로만 사용

---

## 4. 팀원이 각자 해야 할 것

**① 마이그레이션 적용**
```bash
cd backend && alembic upgrade head
```

**② `.env`에서 8줄 삭제** — 안 지우면 **서버가 기동에서 터진다**(`Settings`가 `extra="forbid"`).
```
CANVAS_STROKE_ORDER_PENALTY / CANVAS_SPACING_PENALTY_COEFF / CANVAS_SPACING_PENALTY_MAX
CANVAS_SIZE_PENALTY_COEFF   / CANVAS_SIZE_PENALTY_MAX
IMAGE_SIZE_WEIGHT / IMAGE_SLANT_WEIGHT / IMAGE_LINE_WEIGHT
```
`.env`는 gitignore라 커밋에 안 들어간다. `.env.example`에는 반영해뒀다.

---

## 5. 알아둘 것

**`.omc/` 상태 파일 30건이 이 7개 커밋 이력에 섞여 있다.** AI 코딩 도구가 만드는 세션 상태
파일인데, `ai/.gitignore`에만 `.omc/`가 있고 루트에 없어서 `backend/`·`frontend/` 쪽이 걸러지지
않았다. 루트 `.gitignore`를 추가하고 추적에서 뺐으므로 **앞으로는 안 들어간다.** 이미 들어간
이력은 남는다(작은 JSON이라 실害는 없다).

**되돌린 것**: 통계 예외 처리(§3-1)를 한 번 고쳤다가 되돌렸다. 지금 `analytics_service.dart`에는
**주석만** 있고 동작은 원래 그대로다.
