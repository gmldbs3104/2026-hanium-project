# DATA_FLOW — 세 파트가 주고받는 값 (개념 대조표)

> **AI · 백엔드 · 프론트**가 각각 어떤 값을 다루고, 어디서 값이 사라지는지를 변수 이름 없이
> 개념으로 정리한 문서. 2026-08-06 최초 전수 대조 → **2026-08-12 `feature/ai-integration`
> 통합본 실제 코드로 재검증**(§4~§6·§8의 상태 열).
> **이미지 모드 = §1~§3(값)·§4~§6(불일치·논의) / 캔버스 모드 = §7(값)·§8(불일치).**
> §4의 3·5번은 캔버스 건이고, 7번은 두 모드 공통이다.
> 함수 시그니처·JSON 형식은 [AI_MODEL_INTERFACE.md](AI_MODEL_INTERFACE.md),
> 남은 일·논의는 [STATUS.md](STATUS.md).

**한 줄 요약**: AI가 가장 많이 보내고, 백엔드가 **일부를** 버리고, 프론트가 또 버린다.
08-09~11에 §4의 3~7번과 §8 A~F가 해결돼 "절반을 버린다"는 더 이상 맞지 않는다.

> **이 문서는 두 곳에 있다** — AI 작업본 `ai/DATA_FLOW.md`와 통합본 루트 `DATA_FLOW.md`.
> 2026-08-12에 **양쪽 내용을 같게 맞췄다.** 한쪽만 고치면 다시 갈라지므로 함께 고칠 것.
> (08-09~11 사이에 갈라져 있었고, 루트 쪽 §4 표의 4~7번이 실제로는 해결됐는데 "미해결"로
> 남아 있었다.)

---

## 1. AI가 다루는 값 (이미지 모드)

### 1.1 전처리

| 값 | 내용 |
|---|---|
| **정리된 흑백 이미지** | 원본 사진에서 종이색·조명·비침·괘선을 걷어내고 잉크만 남긴 흑백 이미지. 기울어진 사진은 수평으로 돌려놓고, 너무 작으면 확대. **이후 모든 좌표의 기준** |
| **이미지 크기** | 위 정리된 이미지의 가로·세로. **원본 사진 크기가 아니다** |
| **촬영 품질 점수** | 0~100. 초점이 맞았는지(선명도) + 글씨와 종이가 뚜렷이 구분되는지(대비) + 잉크와 배경이 두 덩어리로 깨끗이 갈리는지(흑백 분리 가능성) |
| **재촬영 필요 여부** | 품질 40점 미만이면 필요. 가로세로 비율이 극단적일 때도 |
| **처리 방식** | 진한 글씨가 있으면 **비침 제거 모드**, 전체가 연한 글씨면 **보존 모드**. 보존 모드는 연한 글씨를 살리는 대신 **비침이 함께 남을 수 있다** |

### 1.2 탐지 — 글자마다

| 값 | 내용 |
|---|---|
| **글자 번호** | 읽기 순서(좌상단→우하단)로 매김 |
| **위치와 크기** | 글자를 감싼 네모의 좌상단 좌표와 가로·세로 |
| **기울어진 각도** | 그 글자의 세로획이 수직에서 몇 도 기울었나 |
| **각도 신뢰 여부** | ㅇ처럼 세로획이 없는 글자는 "측정 불가"로 표시 |
| **탐지 신뢰도** | 현재 모든 글자가 같은 값이라 실질적으로 쓸모 없음 |

### 1.3 분석 — 채점 5지표

| 지표 | 무엇을 보나 | 우수 / 불량 | 측정 조건 |
|---|---|---|---|
| 글자 높이 균일성 | 글자 높이가 들쭉날쭉한가 | 10% 미만 / 20% 이상 | — |
| **문장 기울기** | 한 줄의 글자 중심을 이은 선이 수평에서 몇 도 벗어났나 | 3° 미만 / 7° 이상 | **한 행에 3글자 이상** |
| 자간 균등성 | 글자 사이 간격이 고른가 (띄어쓰기 제외) | 15% 미만 / 30% 이상 | 인접 글자 쌍 필요 |
| 행간 균등성 | 줄과 줄 사이 간격이 고른가 | 10% 미만 / 20% 이상 | **줄이 2개 이상** |
| 기준선 이탈도 | 한 줄 안에서 글자가 기준선 위아래로 얼마나 튀나 | 5% 미만 / 15% 이상 | — |

**종합 점수와 등급** — 위 5개를 가중 평균. 높이·기준선·행간이 3, 기울기·자간이 2.
**못 잰 지표는 빼고** 계산한다. 80점 이상 우수 / 40점 이상 보통 / 미만 불량.

> ⚠️ 문서에는 가중치가 "3:2:1"로 적혀 있으나 실제 코드는 **3:3:3:2:2**다. 1짜리는 없다.

그 밖에 분석이 내놓는 값:

- **기울기 관련** — 평균 각도 / 흔들림 정도 / 전체 방향(올라감·수평·내려감)
- **지표별 상세 근거** — 각 지표의 실제 측정값·등급·점수, 그리고 **못 쟀으면 그 이유**
- **명료도 경고**(점수 미반영) — 흘려 쓰거나 옆 글자와 이어 써서 또렷이 구분되지 않는 글자 수.
  글씨가 너무 작은 사진에서는 모델 한계일 수 있어 경고를 생략한다
- **규범 이탈 3축**(점수 미반영, 경고만) — ① 줄이 수평에서 7° 넘게 기울었나
  ② 띄어쓰기가 완전히 사라졌나 ③ 줄과 줄이 겹치나
- **피드백 문구** — AI가 5지표 전부에 대해 자체 지적 문장을 만든다

### 1.4 분석 — 글자마다

| 값 | 내용 |
|---|---|
| **크기 비율** | 같은 줄 글자들의 중앙값 대비 몇 배 (1.5배 초과=큼, 0.65 미만=작음) |
| **각도** | 탐지 단계 각도 그대로 |
| **이상 표시 3종** | 크기 이상 / 각도 이상 / 흐릿함 |

---

## 2. 백엔드가 다루는 값 (이미지 모드)

### 2.1 내보내는 것

- **세션 번호** — 10분간 유효. 전처리 결과를 서버에 물고 있다가 다음 단계에서 꺼낸다
- **이미지 크기, 품질 점수, 재촬영 여부** — AI 것 그대로
- **전처리된 이미지** — 오버레이용으로 보낸다 (**2026-08-09부터 프론트가 실제로 쓴다**)
- **전처리 방식**(연한 글씨 보존 모드였나) — 2026-08-09 추가. **백엔드는 보내는데 프론트가 안 읽는다**
- **원본 사진 저장 주소** — S3. 2026-07-05(SFR-009)에 실제로 붙었다
- **글자 번호와 위치·크기**
- **점수 3개** — 크기 균일성 / 기울기 일관성 / 줄 정렬
- **자간·행간 점수** — 2026-08-09 응답 추가 → **2026-08-12 DB·대시보드까지 연결**
- **종합 점수와 등급**
- **평균 기울기 각도, 전체 기울기 방향**
- **흐릿함 경고**
- **글자별** — 크기 편차(퍼센트로 변환), 각도
- **피드백 문구 목록** — 점수 3개를 잘라 만든다.
  **60 미만이면 경고, 85 이상이면 칭찬, 그 사이는 아무 문구도 안 나온다**
- **달성 메시지** — 종합 점수 구간별 4단계 고정 문구

### 2.2 저장하고 쌓는 것

- 세션별 점수 기록 — **2026-08-12부터 이미지 5지표 전부**(종전 3개)
- 캔버스 글자별 **속도 프로필·교정 표시**도 저장(2026-08-12). 필압은 프론트가 1.0 고정이라 제외
- 기간별 평균 점수와 향상률
- **취약 항목 순위** — 항목별 평균 점수를 낮은 순으로, 하위 10개
- **레벨** — 누적 세션 수 기준
- **연속 출석일** — 하루에 한 번 이상 연습한 날의 연속 일수

### 2.3 버리는 것 (2026-08-12 기준)

- **못 잰 지표의 "사유 문구"** — 점수는 이제 `None`으로 정확히 전달되지만(§4-1 해결),
  `{"skipped": "행이 부족해 행간 평가 생략"}` 같은 **왜 못 쟀는지는 여전히 응답에 안 실린다.**
  화면에 "미측정" 옆에 이유를 보여주려면 이걸 살려야 한다
- **AI가 만든 피드백 문구** — 자기 문구로 새로 쓴다
- **규범 이탈 3축**
- **글자별 이상 표시 3종**

**더 이상 버리지 않는 것** (2026-08-09 연결, §5 참고)

- ~~전처리 방식~~ → `preservation_mode`로 응답에 실린다 (다만 프론트가 안 읽음)
- ~~글자별 각도·각도 신뢰 여부·탐지 신뢰도~~ → `/detect`가 원래 다 보내고 있었다(문서가 낡았던 것)
- ~~자간·행간 점수~~ → 응답·DB·대시보드 전부 연결됨(2026-08-12, 마이그레이션 `b3f1c27a9d40`)

---

## 3. 프론트가 다루는 값 (이미지 모드)

### 3.1 받아서 쓰는 것

- **재촬영 필요 여부** — 2026-08-09부터 서버 판정(`retake_required`)을 그대로 쓴다
  (그 전엔 품질 점수로 프론트가 다시 계산했다)
- **전처리된 이미지** — 2026-08-09부터 오버레이 배경으로 실제 사용
- **글자 위치와 크기** — 오버레이용
- **글자별 각도 신뢰 여부** — 2026-08-09 파싱 추가
- **종합 점수 / 달성 메시지 / 피드백 문구 목록**
- **서버 오류 사유** — 2026-08-09부터 FastAPI `detail`을 뽑아 화면에 띄운다

### 3.2 화면에 그리는 것

- **종합 점수** — 큰 숫자 + "점"
- **달성 메시지** — 그 아래 한 줄
- **피드백 문구 목록** — 글자 번호 + 문장
- **사진 위 글자 네모** — 오버레이

### 3.3 버리는 것

**분석 단계 응답 전체** — 항목별 점수 3개(+2026-08-09에 늘어난 자간·행간), 평균 기울기 각도,
전체 기울기 방향, 등급, 흐릿함 경고, 글자별 크기 편차·각도.

분석은 **서버 캐시를 채우려고만** 호출하고, 점수는 피드백 단계에서만 받는다.

**전처리 방식**(`preservation_mode`)도 안 읽는다 — 유령 박스 경고를 띄우려면 여기서 시작해야 한다.

---

## 4. 반드시 고쳐야 하는 불일치

지금 **사용자에게 거짓 정보가 나가거나 기능이 실패하는 것.**

| # | 안 맞는 곳 | 지금 생기는 일 | 고칠 파트 | 상태 (2026-08-12 검증) |
|---|---|---|---|---|
| 1 | ~~못 잰 지표가 만점으로 흘러간다~~ | ~~재지도 않은 지표로 칭찬하고 대시보드 평균까지 오염~~ | AI + 백엔드 | ✅ **2026-08-12 해결** — AI가 `.get("score")`로 `None`을 그대로 내보내고, 백엔드가 5지표를 모두 `_metric_score()`로 읽어 `None`을 유지한다. 피드백 문구도 `None`이면 생성하지 않는다 |
| 2 | ~~대시보드가 "값 없음"을 **0점**으로 집계한다~~ | ~~1번을 고쳐도 거짓말이 "완벽"에서 "최악"으로 뒤집힘~~ | 백엔드 | ✅ **2026-08-12 해결** — `or 0` 제거, `items` 딕셔너리에서 `None`인 항목을 **아예 뺀다**(집계 대상 밖) |
| 3 | ~~캔버스가 어떤 글자인지 전달하지 않는다~~ | ~~모든 글자가 "획이 1개 더 많습니다". 크기 기준도 임의값 100×100~~ | 프론트 + 백엔드 | ✅ 08-11 `ab9de5a` (§8-A와 함께) |
| 4 | ~~오버레이를 원본 사진 위에 그린다~~ | ~~괘선 종이에서 반 글자 밀림~~ | 프론트 | ✅ 08-09 `38a6df4` |
| 5 | ~~캔버스 크기값이 소수로 나간다~~ | ~~화면 크기에 따라 제출 실패~~ | 프론트 | ✅ 08-09 `38a6df4` (회귀 테스트 포함) |
| 6 | ~~카메라를 못 잡으면 더미 데이터를 보낸다~~ | ~~권한 거부 시 422~~ | 프론트 | ✅ 08-09 `38a6df4` |
| 7 | ~~서버가 보낸 오류 사유를 화면에서 버린다~~ | ~~사용자가 원인을 알 수 없음~~ | 프론트 | ✅ 08-09 `38a6df4` (`ApiException.serverMessage`) |

> **1·2는 반드시 한 세트로 고친다.** 1만 고치면 만점이 `None`이 되고, 그 `None`을 2번이 0으로
> 집계해 거짓말이 "완벽"에서 "최악"으로 뒤집힌다. 순서가 아니라 동시다.
>
> 3번 해결 방식은 **DB(11,172자 시드) 경유가 아니었다** — §8-A를 고치면서 AI의 유니코드 산술
> 기반 획순 계산(`ai/canvas/stroke_standards.py`)으로 대체됐다. DB의 `get_standard`는 이제 안 쓰인다.

---

## 5. 연결하면 좋은 불일치

지금 당장 망가지진 않지만 **AI가 보내는 값이 버려지고 있는 것.**

| # | 안 맞는 곳 | 고칠 파트 | 상태 (2026-08-12 검증) |
|---|---|---|---|
| 8 | ~~자간·행간 점수가 백엔드에 도달하지 않는다~~ | 백엔드 | ✅ **2026-08-12 완결** — 08-09에 응답까지, 08-12에 **DB 컬럼 + 대시보드 집계**까지. 분석 화면 "더 연습이 필요한 항목"에 자간·행간이 실제로 뜬다(프론트 수정 0줄) |
| 9 | 탐지 각도·신뢰도가 프론트에 도달하지 않는다 | 백엔드 | ✅ **원래 돼 있었다**(문서가 틀렸음). `/detect`가 `angle`/`angle_reliable`/`confidence`를 이미 보내고 있었고, 프론트가 `angle_reliable`만 안 읽어 08-09에 파싱 추가 |
| 10 | 전처리 방식이 응답에 안 실린다 — 유령 박스 경고의 재료 | 백엔드 | ⚠️ **반쪽 해결** 08-09 — `ImagePreprocessResponse.preservation_mode` 추가. **프론트가 안 읽어** 경고는 여전히 화면에 없다 |
| 11 | ~~설정 파일의 이미지 점수 가중치 3개가 아무 데서도 안 쓰인다~~ | 백엔드 | ✅ **2026-08-12 해결** — `config.py`와 `.env`·`.env.example`에서 삭제(§8-G와 한 건으로 처리). 채점은 AI가 소유하므로 백엔드에 계수를 두지 않는다 |

---

## 6. 맞추는 것 외에 남는 것

### 6.1 맞추면서 함께 정할 것

- 항목별 점수를 화면에 보여줄 것인가 — **미정**(백엔드는 5개를 보내는데 프론트가 다 버린다)
- 자간·행간을 사용자에게 노출할 것인가 — **미정**(응답에는 실려 있음, §5-8)
- ~~오버레이 배경 — 이진 전처리 이미지 vs 회전만 한 컬러~~ → ✅ **이진 전처리 이미지로 확정**
  (08-09 프론트 반영 완료)

### 6.2 별도 논의

| 항목 | 정할 것 | 상태 (2026-08-12) |
|---|---|---|
| 계정 삭제 | 엔드포인트·응답 스펙 | 미구현 — 백엔드에 **DELETE/PATCH 라우트가 하나도 없다** |
| 유령 박스 | 경고 문구만 띄울지, 탐지 로직을 만들지 | 재료(`preservation_mode`)만 생김. 프론트 미사용 |
| 추천 연습 예문 | 누가 만들지 | 여전히 빈 배열 (`dashboard_service.py:237` TODO) |
| ~~이미지 저장~~ | ~~S3를 붙일지, 뺄지~~ | ✅ **끝난 논의** — 07-05 `b6bb388`(SFR-009)에 이미 붙었다 |

### 6.3 시점이 정해진 것

- **속도** — 팀 논의 진행 중
- **캔버스 실사용자 데이터 수집** — 앱 완성 후
- **임계값·가중치 숫자**(7°, 3:3:3:2:2, 60/85) — 데이터 수집 후

### 6.4 프론트가 앞서 나가 생긴 새 불일치 (2026-08-12 발견)

프론트가 **백엔드에 없는 엔드포인트를 이미 호출하도록** 만들어져 있다. §4와 달리 AI 값 흐름과는
무관하지만, 지금 사용자에게 실패로 보이는 것이라 함께 적어둔다.

| 프론트가 부르는 것 | 백엔드 | 지금 생기는 일 |
|---|---|---|
| `DELETE /api/v1/user/history` (설정 → 데이터 초기화) | 없음 | 버튼을 누르면 실패 |
| 프로필 수정 API (`PATCH /api/v1/auth/profile` 류) | 없음 | 닉네임·사진이 `SharedPreferences`로 **기기에만** 저장 |

그 밖에 프론트가 "백엔드 필요"로 표시해둔 것: 성취 배지(카탈로그·판정·저장 전부 없음),
세션 단위 기록 조회 엔드포인트, 글자 단위 취약 항목(`weak_chars`), 알림 발송(FCM).

---

## 7. 캔버스 모드 — 세 파트가 다루는 값

### 7.1 AI가 다루는 값

**획 그룹핑**

| 값 | 내용 |
|---|---|
| **글자 번호** | 읽기 순서 |
| **글자 영역** | 묶인 획을 감싼 네모 |
| **묶인 획 목록 · 획 개수** | 어떤 획들이 한 글자인지 |
| **그룹핑 신뢰도 · 저신뢰 표시** | 거리·시간으로 묶은 확신도 |

**획순·자간·크기 분석** — 입력으로 **목표 글자를 받는다**(제시형 전제)

| 값 | 내용 |
|---|---|
| **획순 결과** | 기대 순서 / 실제 매칭 / 오류 개수 / 교정 문구 / 표준 안내. **위치+모양 기하 비교라 순서 오류까지 잡는다** |
| **자간 편차** | 평균 글자폭의 40%를 기준으로 |
| **크기 편차** | 같은 줄 글자 **중앙값 대비** |
| **필압 프로필 · 속도 프로필** | 필압 센서 없는 기기에선 1.0 고정 |
| **종합 점수** | 크기·자간·획순 감점 |
| **교정 플래그** | 너무 큼/작음 · 너무 붙음/벌어짐 · 획순 오류 |

**표준 데이터** — 11,172자 표준 획순 + 복수 정본(ㅌ 등)

### 7.2 백엔드가 다루는 값 (2026-08-11 `ab9de5a`로 전면 교체)

**분석 본체는 이제 AI 것을 그대로 쓴다**

`routes/handwriting.py`의 `analyze_canvas_detail()`이 `ai_adapters.analyze_canvas_writing`
(= `ai/canvas/canvas_quality_analyzer.analyze_canvas_writing`)을 **직접 호출**한다.
자체 구현이던 `services/canvas_analysis.py`(`get_standard`/`analyze_stroke_order`/
`analyze_spacing`/`analyze_size`/`calculate_overall_score`)는 **파일째 삭제됐다**.

- 획순 — **위치+모양 기하 비교**(`target_text`가 있을 때). 개수 비교가 아니다
- 크기 편차 — AI 기준(같은 줄 **중앙값 대비**)으로 통일
- 자간 편차 — AI 기준(평균 글자폭 **40%**)으로 통일
- 종합 점수 · 교정 플래그 · 필압/속도 프로필 · 복수 정본 안내 — 전부 AI 값

**백엔드가 여전히 자체로 하는 것**

- 그룹핑 — 중심 거리 + 시간 간격 (`services/stroke_grouping.py`, bounding_box 키는
  08-11에 `w/h` → `width/height`로 통일)
- 피드백 문구 — 글자마다 획순+자간+크기 3문장을 이어붙임
- 달성 메시지 — 종합 점수 구간별 4단계

**저장·누적** — 글자별 획순 결과·자간·크기·종합 점수 **4개뿐**(DB 컬럼 그대로) /
대시보드 항목 3개(획순·자간·크기). ⚠️ 대시보드는 저장된 종합 점수를 쓰지 않고
**편차에서 항목 점수를 다시 계산**한다 — 그때 쓰는 계수가 AI와 다르다(§8-G).

**여전히 응답에만 있고 안 쌓이는 것** — 필압·속도 프로필, 교정 플래그.
DB 컬럼·마이그레이션이 없다 (§8-B).

### 7.3 프론트가 다루는 값

**보내는 것** — 획 좌표(x·y·**필압**·타임스탬프) / 캔버스 크기(08-09부터 **정수**) / 획 개수 /
**목표 글자**(08-09에 프론트가 먼저 보내기 시작 → 08-11에 백엔드가 받기 시작)

**화면에 그리는 것** — 종합 점수 / 달성 메시지 / 피드백 문구 목록 / 글자 영역

**버리는 것** — 분석 응답 전체 (이미지 모드와 동일)

---

## 8. 캔버스 모드 불일치 (2026-08-06 발견 → 08-11 일괄 해결)

| # | 안 맞는 곳 | 지금 생기는 일 | 상태 (2026-08-12 검증) |
|---|---|---|---|
| **A** | **AI의 캔버스 분석기가 연결된 적이 없다** — 계약이 캔버스 쪽은 스텁 2개만 노출, 백엔드는 자체 구현 | 획순이 **획 개수 비교만** | ✅ 08-11 `ab9de5a` — `analyze_canvas_writing`을 직접 호출 |
| B | 속도 프로필이 버려진다 | 필기 습관 지표가 전무 | ✅ **2026-08-12 저장까지 완료**(`speed_profile` 컬럼). ⚠️ **필압은 제외** — 프론트가 `pressure: 1.0`을 하드코딩해 전부 같은 값이라 쌓아도 볼 게 없다 |
| C | ~~교정 플래그가 버려진다~~ | ~~백엔드 자체 문구로 대체~~ | ✅ **2026-08-12 저장까지 완료**(`correction_flags` 컬럼) |
| D | 복수 정본 안내가 전달 경로 없음 | ㅌ 등 인정 표준도 오류 처리 | ✅ `stroke_order_result.notes`로 전달 |
| E | **자간 기준이 다르다** (AI 40% vs 백엔드 고정값) | 같은 글씨에 다른 점수 | ✅ AI 기준(40%)으로 통일 |
| F | **크기 기준 철학이 다르다** (상대 vs 절대) | 두 모드가 다른 잣대 | ✅ AI 기준(상대)으로 통일 — 이미지 모드와 잣대 일치 |
| G | ~~감점 계수가 두 벌이다~~ | ~~같은 필기에 피드백 화면 점수와 대시보드 항목 점수가 어긋난다~~ | ✅ **2026-08-12 해결** — 계산을 AI의 `canvas_item_scores()` 한 곳으로 모으고 백엔드 계수 5개를 삭제. 대시보드가 그 함수를 호출한다 |

- §4의 **3번(글자 미전달)은 A의 한 증상**이었다 — `CanvasAnalyzeRequest.target_text` 추가 +
  세션 캐시 저장으로 함께 해결됐다. 프론트는 08-09부터 이미 보내고 있었고 백엔드가 몰라서
  무시하던 상태였다.
- A를 고칠 때 걸렸던 `w/h` vs `width/height` 키 불일치는 `width`/`height`로 통일하며 해결.
- ✅ STATUS·CLAUDE.md의 **"스텁 대신 `analyze_stroke_order_by_position`이 대신 동작한다"** 는
  08-06까지 **AI 패키지 안에서만 참**이었으나, **08-11부터 통합본에서도 참**이다.
  (12막에서 "⛔ 통합본에선 거짓"으로 정정한 서술이 다시 낡았다 — `ai/CLAUDE.md`·STATUS·
  HANDOFF 재정정 필요.)

### 8.1 캔버스에 아직 남은 것

- ✅ **B·C 영속화 완료(2026-08-12, `b3f1c27a9d40`)** — `speed_profile`·`correction_flags` 컬럼 추가.
  ⚠️ **필압은 일부러 뺐다** — 프론트(`canvas_input_screen.dart:99`·`sentence_practice_screen.dart:83`)가
  `pressure: 1.0`을 하드코딩해서 보내 전부 같은 값이다. 진짜 값을 읽게 고친 뒤에 컬럼을 만든다.

### 8.2 G — 감점 계수가 두 벌이던 것 (2026-08-12 발견 → 같은 날 해결)

§8-A로 채점을 AI에 넘겼는데 **대시보드만 옛 계수로 따로 계산하는 경로가 남아 있었다.**
저장되는 `overall_score`는 AI가 만든 값인데, 대시보드는 그 점수를 안 쓰고
`dashboard_service._canvas_item_scores()`에서 저장된 편차를 설정 계수로 재계산했다.

| 감점 | AI — 세션 점수 | 백엔드 설정 — 대시보드(옛) |
|---|---|---|
| 크기 | `편차 × 0.8`, 최대 50 | `편차 × 0.5`, 최대 30 |
| 자간 | `편차 × 0.3`, 최대 30 | `편차 × 0.5`, 최대 30 |
| 획순 | `오류 × 15`, 최대 40 | `오류 × 10`, 상한 없음 |

**해결** — 계산을 `ai/canvas/canvas_quality_analyzer.canvas_item_scores()` **한 곳**으로 모으고,
백엔드는 `ai_adapters`를 통해 그 함수를 호출한다. `config.py`·`.env`·`.env.example`의
캔버스 계수 5개와 이미지 가중치 3개(§5-11)를 함께 지웠다.

- ⚠️ **`.env`에도 반드시 지워야 한다** — `Settings`가 `extra="forbid"`라 키가 남아 있으면
  **앱이 기동에서 터진다.** 실제로 코드만 지웠을 때 `extra_forbidden`으로 실패했다.
- 함께 고친 것: 획순은 목표 글자를 모르면 `None`(미측정)이다. 종전 백엔드는
  `error_count`를 0으로 보고 **만점**을 줬다 — §4-1과 같은 "안 잰 걸로 칭찬"이었다.
- 검증: 같은 세션에서 결과 화면 항목 점수와 대시보드 항목 점수가 **완전히 일치**.
  종합 점수 산식도 리팩터링 전과 동일함을 확인(크기 -40% 글자: 옛 백엔드 80.0 vs AI 68.0 → 이제 68.0로 통일).

---|---|---|
| 크기 | `편차 × 0.8`, 최대 50 | `편차 × 0.5`, 최대 30 |
| 자간 | `편차 × 0.3`, 최대 30 | `편차 × 0.5`, 최대 30 |
| 획순 | `오류 × 15`, 최대 40 | `오류 × 10`, 상한 없음 |

→ 같은 필기인데 **피드백 화면 점수와 대시보드 취약 항목 점수가 서로 다른 잣대**로 나온다.
`config.py`의 `canvas_*_penalty*` 5개는 "죽은 설정"은 아니지만 **반만 살아 있다** —
사용자가 쓰고 바로 보는 점수에는 이제 영향이 없고 대시보드 집계에만 영향을 준다.

⚠️ §5-11(이미지 가중치가 아무 데서도 안 쓰임)과 **같은 뿌리**다: 채점 주체가 백엔드에서 AI로
옮겨갔는데 백엔드 쪽 계수를 정리하지 않았다. 두 건을 **"채점은 누가 소유하나"** 하나로 묶어
결정하는 게 맞다.
- **자모 단독 획순 채점 불가** — `decompose_syllable`이 완성형 음절만 분해해서
  ㄱ·ㅏ 같은 단독 자모는 서버 에러 없이 `stroke_order_result: null`로 빠지고 크기·자간만 채점된다.

---

## 참고 — 확인된 사실

- **전처리 리사이즈는 속도와 무관하다.** 탐지기가 글자 높이 ~90px에 맞춰 입력을 다시 스케일하므로
  배율이 상쇄된다. 다운스케일해도 추론 시간은 안 줄고 획만 깨진다.
- **획순 표준은 문헌이 아니라 한글 조합 규칙(유니코드 산술)으로 생성**했다. 문헌 근거는
  복수 정본(ㅌ 등) 쪽이다. 08-11 이후 실사용 경로가 쓰는 것도 **DB 시드가 아니라 이쪽**이다.
- **캔버스 LSTM 스텁 2곳(`lstm_refine_grouping`·`lstm_analyze_stroke_order`)은 지금도 스텁이다.**
  08-11 해결은 스텁을 고친 게 아니라 `analyze_canvas_writing`을 **"추가 제공" 함수로 새로 노출**해
  우회한 것이다 — 3함수 계약 자체는 손대지 않았다.

---

## 남은 일 요약 (2026-08-12 기준, 단일 출처는 STATUS.md)

**코드** — ① §4-1+2 세트(못 잰 지표 만점 → 집계 제외) → ② §6.4 없는 API 2개 →
③ **§5-11 + §8-G 채점 소유권 정리**(둘은 한 건) → ④ §5-8 + §8-B/C 영속화 묶어서.

**문서** — 이 문서와 통합본 루트 `DATA_FLOW.md` 두 벌 통합 / 루트 §4 표에 4~7번 ✅ 표기 /
`ai/CLAUDE.md`·STATUS·HANDOFF의 "⛔ 통합본에 연결 안 됨" 재정정 / 가중치 표기
`3:2:1` → `3:3:3:2:2`(`handwriting_analyzer.py:142` 주석 포함).
