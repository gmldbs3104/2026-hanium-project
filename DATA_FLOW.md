# DATA_FLOW — 세 파트가 주고받는 값 (개념 대조표)

> **AI · 백엔드 · 프론트**가 각각 어떤 값을 다루고, 어디서 값이 사라지는지를 변수 이름 없이
> 개념으로 정리한 문서. 2026-08-06 실제 코드 전수 대조.
> **이미지 모드 = §1~§3(값)·§4~§6(불일치·논의) / 캔버스 모드 = §7(값)·§8(불일치).**
> §4의 3·5번은 캔버스 건이고, 7번은 두 모드 공통이다.
> 함수 시그니처·JSON 형식은 [AI_MODEL_INTERFACE.md](AI_MODEL_INTERFACE.md),
> 남은 일·논의는 [STATUS.md](STATUS.md).

**한 줄 요약**: AI가 가장 많이 보내고, 백엔드가 절반을 버리고, 프론트가 또 버린다.

---

## 1. AI가 다루는 값 (이미지 모드)

### 1.1 전처리

| 값 | 내용 |
|---|---|
| **정리된 흑백 이미지** | 원본 사진에서 종이색·조명·비침·괘선을 걷어내고 잉크만 남긴 흑백 이미지. 기울어진 사진은 수평으로 돌려놓고, 너무 작으면 확대. **이후 모든 좌표의 기준** |
| **이미지 크기** | 위 정리된 이미지의 가로·세로. **원본 사진 크기가 아니다** |
| **촬영 품질 점수** | 0~100. 초점이 맞았는지(선명도) + 글씨와 종이가 뚜렷이 구분되는지(대비) + 잉크와 배경이 두 덩어리로 깨끗이 갈리는지(흑백 분리 가능성) |
| **재촬영 필요 여부** | 품질 40점 미만이면 필요. 가로세로 비율이 극단적일 때도 |
| **처리 방식** | 진한 글씨가 있으면 **비침 제거 모드**, 전체가 연한 글씨면 **보존 모드**. 보존 모드는 연한 글씨를 살리는 대신 **비침이 함께 남을 수 있다** |

### 1.2 탐지 — 글자마다

| 값 | 내용 |
|---|---|
| **글자 번호** | 읽기 순서(좌상단→우하단)로 매김 |
| **위치와 크기** | 글자를 감싼 네모의 좌상단 좌표와 가로·세로 |
| **기울어진 각도** | 그 글자의 세로획이 수직에서 몇 도 기울었나 |
| **각도 신뢰 여부** | ㅇ처럼 세로획이 없는 글자는 "측정 불가"로 표시 |
| **탐지 신뢰도** | 현재 모든 글자가 같은 값이라 실질적으로 쓸모 없음 |

### 1.3 분석 — 채점 5지표

| 지표 | 무엇을 보나 | 우수 / 불량 | 측정 조건 |
|---|---|---|---|
| 글자 높이 균일성 | 글자 높이가 들쭉날쭉한가 | 10% 미만 / 20% 이상 | — |
| **문장 기울기** | 한 줄의 글자 중심을 이은 선이 수평에서 몇 도 벗어났나 | 3° 미만 / 7° 이상 | **한 행에 3글자 이상** |
| 자간 균등성 | 글자 사이 간격이 고른가 (띄어쓰기 제외) | 15% 미만 / 30% 이상 | 인접 글자 쌍 필요 |
| 행간 균등성 | 줄과 줄 사이 간격이 고른가 | 10% 미만 / 20% 이상 | **줄이 2개 이상** |
| 기준선 이탈도 | 한 줄 안에서 글자가 기준선 위아래로 얼마나 튀나 | 5% 미만 / 15% 이상 | — |

**종합 점수와 등급** — 위 5개를 가중 평균. 높이·기준선·행간이 3, 기울기·자간이 2.
**못 잰 지표는 빼고** 계산한다. 80점 이상 우수 / 40점 이상 보통 / 미만 불량.

> ⚠️ 문서에는 가중치가 "3:2:1"로 적혀 있으나 실제 코드는 **3:3:3:2:2**다. 1짜리는 없다.

그 밖에 분석이 내놓는 값:

- **기울기 관련** — 평균 각도 / 흔들림 정도 / 전체 방향(올라감·수평·내려감)
- **지표별 상세 근거** — 각 지표의 실제 측정값·등급·점수, 그리고 **못 쟀으면 그 이유**
- **명료도 경고**(점수 미반영) — 흘려 쓰거나 옆 글자와 이어 써서 또렷이 구분되지 않는 글자 수.
  글씨가 너무 작은 사진에서는 모델 한계일 수 있어 경고를 생략한다
- **규범 이탈 3축**(점수 미반영, 경고만) — ① 줄이 수평에서 7° 넘게 기울었나
  ② 띄어쓰기가 완전히 사라졌나 ③ 줄과 줄이 겹치나
- **피드백 문구** — AI가 5지표 전부에 대해 자체 지적 문장을 만든다

### 1.4 분석 — 글자마다

| 값 | 내용 |
|---|---|
| **크기 비율** | 같은 줄 글자들의 중앙값 대비 몇 배 (1.5배 초과=큼, 0.65 미만=작음) |
| **각도** | 탐지 단계 각도 그대로 |
| **이상 표시 3종** | 크기 이상 / 각도 이상 / 흐릿함 |

---

## 2. 백엔드가 다루는 값 (이미지 모드)

### 2.1 내보내는 것

- **세션 번호** — 10분간 유효. 전처리 결과를 서버에 물고 있다가 다음 단계에서 꺼낸다
- **이미지 크기, 품질 점수, 재촬영 여부** — AI 것 그대로
- **전처리된 이미지** — 오버레이용으로 보낸다 (**프론트가 안 쓴다**)
- **원본 사진 저장 주소** — S3. 미설정이면 없음
- **글자 번호와 위치·크기**
- **점수 3개** — 크기 균일성 / 기울기 일관성 / 줄 정렬
- **종합 점수와 등급**
- **평균 기울기 각도, 전체 기울기 방향**
- **흐릿함 경고**
- **글자별** — 크기 편차(퍼센트로 변환), 각도
- **피드백 문구 목록** — 점수 3개를 잘라 만든다.
  **60 미만이면 경고, 85 이상이면 칭찬, 그 사이는 아무 문구도 안 나온다**
- **달성 메시지** — 종합 점수 구간별 4단계 고정 문구

### 2.2 저장하고 쌓는 것

- 세션별 점수 기록
- 기간별 평균 점수와 향상률
- **취약 항목 순위** — 항목별 평균 점수를 낮은 순으로, 하위 10개
- **레벨** — 누적 세션 수 기준
- **연속 출석일** — 하루에 한 번 이상 연습한 날의 연속 일수

### 2.3 버리는 것

- **전처리 방식**(연한 글씨 보존 모드를 썼는지) — 유령 박스 경고의 재료
- **글자별 각도, 각도 신뢰 여부, 탐지 신뢰도**
- **못 잰 지표의 사유**
- **AI가 만든 피드백 문구** — 자기 문구로 새로 쓴다
- **규범 이탈 3축**
- **글자별 이상 표시 3종**
- ⚠️ **자간·행간 점수 자체** — AI는 5지표로 채점하는데 백엔드가 받는 건 3개뿐이다.
  자간·행간은 **종합 점수 안에 녹아만 있고** 따로 꺼내 보거나 대시보드에 쌓을 수 없다

---

## 3. 프론트가 다루는 값 (이미지 모드)

### 3.1 받아서 쓰는 것

- **품질 점수** — 40점 미만이면 "다시 촬영해주세요". **프론트가 실제로 판단에 쓰는 유일한 값**
- **글자 위치와 크기** — 오버레이용
- **종합 점수 / 달성 메시지 / 피드백 문구 목록**

### 3.2 화면에 그리는 것

- **종합 점수** — 큰 숫자 + "점"
- **달성 메시지** — 그 아래 한 줄
- **피드백 문구 목록** — 글자 번호 + 문장
- **사진 위 글자 네모** — 오버레이

### 3.3 버리는 것

**분석 단계 응답 전체** — 항목별 점수 3개, 평균 기울기 각도, 전체 기울기 방향, 등급,
흐릿함 경고, 글자별 크기 편차·각도.

분석은 **서버 캐시를 채우려고만** 호출하고, 점수는 피드백 단계에서만 받는다.

---

## 4. 반드시 고쳐야 하는 불일치

지금 **사용자에게 거짓 정보가 나가거나 기능이 실패하는 것.**

| # | 안 맞는 곳 | 지금 생기는 일 | 고칠 파트 |
|---|---|---|---|
| 1 | AI가 보내는 "못 쟀다"는 사유를 백엔드가 버리고 만점만 받는다 | 재지도 않은 지표로 **칭찬**하고 대시보드 평균까지 오염 | AI + 백엔드 |
| 2 | 대시보드가 "값 없음"을 **0점**으로 집계한다 | 1번을 고쳐도 거짓말이 "완벽"에서 "최악"으로 뒤집힘 | 백엔드 |
| 3 | 캔버스가 **어떤 글자인지** 전달하지 않는다 (프론트 필드 없음 + 백엔드가 글자 자리에 `None` 하드코딩) | 모든 글자가 **"획이 1개 더 많습니다"**. 크기 기준도 임의값 100×100이라 크기 편차가 무의미 | 프론트 + 백엔드 |
| 4 | 오버레이를 **원본 사진** 위에 그린다 (백엔드가 보내는 전처리 이미지를 안 씀) | 괘선 종이에서 **반 글자 밀림**. 무지 종이 테스트로는 안 잡힘 | 프론트 |
| 5 | 캔버스 크기값이 **소수**로 나간다 | 화면 크기에 따라 제출이 실패 | 프론트 |
| 6 | 카메라를 못 잡으면 **더미 데이터**를 보낸다 | 권한 거부 시에도 발생 → 422 | 프론트 |
| 7 | 서버가 보낸 오류 사유를 화면에서 버린다 | 사용자가 원인을 알 수 없어 대처 불가 | 프론트 |

> 3번은 새로 만들 로직이 없다 — 프론트는 정답 글자를 알고 있고(제시형), DB에 11,172자 획순 표준이 시드돼 있다.

---

## 5. 연결하면 좋은 불일치

지금 당장 망가지진 않지만 **AI가 보내는 값이 버려지고 있는 것.**

| # | 안 맞는 곳 | 고칠 파트 | 상태 |
|---|---|---|---|
| 8 | 자간·행간 점수가 백엔드에 도달하지 않는다 (종합 점수 안에만 녹아 있음) | 백엔드 | ✅ 2026-08-09 해결 — `ImageAnalysisResponse.spacing_uniformity_score`/`line_spacing_uniformity_score` 추가, `analyze_size_angle()`의 `metrics["spacing_uniformity"|"line_spacing_uniformity"]`를 그대로 노출. DB 컬럼·대시보드 적립은 아직 없음(응답 레벨만 연결) |
| 9 | 탐지 각도·신뢰도가 프론트에 도달하지 않는다 (**프론트는 이미 받을 준비 완료**) | 백엔드 | ✅ 이미 해결돼 있었음(문서가 낡아 있었다) — `/detect`가 `angle`/`angle_reliable`/`confidence`를 이미 채워 보냄. 프론트가 `angle_reliable`만 안 읽고 있어서 2026-08-09에 `DetectedChar.angleReliable` 파싱 추가 |
| 10 | 전처리 방식(연한 글씨 보존 모드 여부)이 응답에 안 실린다 — 유령 박스 경고의 재료 | 백엔드 | ✅ 2026-08-09 해결 — `ImagePreprocessResponse.preservation_mode` 추가(`applied_filters`에 `"gentle_stretch"` 포함 여부로 판정) |
| 11 | 설정 파일의 이미지 점수 가중치 3개가 **아무 데서도 안 쓰인다** | 백엔드 (문서화) | 미해결 |

---

## 6. 맞추는 것 외에 남는 것

### 6.1 맞추면서 함께 정할 것

- 항목별 점수를 화면에 보여줄 것인가
- 자간·행간을 사용자에게 노출할 것인가
- 오버레이 배경 — 이진 전처리 이미지 vs 회전만 한 컬러

### 6.2 별도 논의

| 항목 | 정할 것 |
|---|---|
| 계정 삭제 | 엔드포인트·응답 스펙 (백엔드 미구현) |
| 유령 박스 | 경고 문구만 띄울지, 탐지 로직을 만들지 |
| 추천 연습 예문 | 누가 만들지 (현재 항상 빈 배열) |
| 이미지 저장 | S3를 붙일지, 기능을 뺄지 (현재 저장 안 됨) |

### 6.3 시점이 정해진 것

- **속도** — 팀 논의 진행 중
- **캔버스 실사용자 데이터 수집** — 앱 완성 후
- **임계값·가중치 숫자**(7°, 3:3:3:2:2, 60/85) — 데이터 수집 후

---

## 7. 캔버스 모드 — 세 파트가 다루는 값

### 7.1 AI가 다루는 값

**획 그룹핑**

| 값 | 내용 |
|---|---|
| **글자 번호** | 읽기 순서 |
| **글자 영역** | 묶인 획을 감싼 네모 |
| **묶인 획 목록 · 획 개수** | 어떤 획들이 한 글자인지 |
| **그룹핑 신뢰도 · 저신뢰 표시** | 거리·시간으로 묶은 확신도 |

**획순·자간·크기 분석** — 입력으로 **목표 글자를 받는다**(제시형 전제)

| 값 | 내용 |
|---|---|
| **획순 결과** | 기대 순서 / 실제 매칭 / 오류 개수 / 교정 문구 / 표준 안내. **위치+모양 기하 비교라 순서 오류까지 잡는다** |
| **자간 편차** | 평균 글자폭의 40%를 기준으로 |
| **크기 편차** | 같은 줄 글자 **중앙값 대비** |
| **필압 프로필 · 속도 프로필** | 필압 센서 없는 기기에선 1.0 고정 |
| **종합 점수** | 크기·자간·획순 감점 |
| **교정 플래그** | 너무 큼/작음 · 너무 붙음/벌어짐 · 획순 오류 |

**표준 데이터** — 11,172자 표준 획순 + 복수 정본(ㅌ 등)

### 7.2 백엔드가 다루는 값

**AI에서 부르는 것은 2개뿐 — 둘 다 스텁**

- 그룹핑 정제 → 입력 그대로 반환
- 획순 → **획 개수 비교만**

**나머지는 자체 구현**

- 그룹핑 — 중심 거리 + 시간 간격
- 크기 편차 — **표준 높이·너비 대비**
- 자간 편차 — 실제 간격 − **표준 고정값**
- 종합 점수 — 100 − 획순·자간·크기 감점 (설정 파일 가중치, **실제로 작동**)
- 피드백 문구 — 글자마다 획순+자간+크기 3문장을 이어붙임
- 달성 메시지 — 종합 점수 구간별 4단계

**저장·누적** — 글자별 획순 결과·자간·크기·종합 점수 / 대시보드 항목 3개(획순·자간·크기)

**버리는 것** — AI의 필압·속도 프로필, 교정 플래그, 복수 정본 안내 (**애초에 호출 경로가 없다**)

### 7.3 프론트가 다루는 값

**보내는 것** — 획 좌표(x·y·**필압**·타임스탬프) / 캔버스 크기 / 획 개수. **목표 글자는 안 보낸다**

**화면에 그리는 것** — 종합 점수 / 달성 메시지 / 피드백 문구 목록 / 글자 영역

**버리는 것** — 분석 응답 전체 (이미지 모드와 동일)

---

## 8. 캔버스 모드 불일치 (2026-08-06)

| # | 안 맞는 곳 | 지금 생기는 일 |
|---|---|---|
| **A** | **AI의 캔버스 분석기가 연결된 적이 없다** — 3함수 계약이 캔버스 쪽은 **스텁 2개**(그룹핑 정제·획순)만 노출한다. 백엔드는 그룹핑·크기·자간·점수를 **자체 구현**했다 | 획순이 **획 개수 비교만**. 위치·순서 오류를 못 잡는다 |
| B | 필압·속도 프로필이 버려진다 (프론트는 필압을 보내는 중) | 필기 습관 지표가 전무 |
| C | 교정 플래그가 버려진다 (너무 큼·작음·너무 붙음·벌어짐·획순 오류) | 백엔드 자체 문구로 대체 |
| D | 복수 정본 안내가 전달 경로 없음 | ㅌ 등에서 인정된 표준을 써도 오류 처리 |
| E | **자간 기준이 다르다** — AI는 평균 글자폭의 40%, 백엔드는 표준 테이블 고정값 | 같은 글씨에 다른 점수 |
| F | **크기 기준 철학이 다르다** — AI는 같은 줄 중앙값 대비(상대), 백엔드는 표준 높이 대비(절대) | 이미지 모드는 상대 기준 → **두 모드가 다른 잣대** |

- §4의 **3번(글자 미전달)은 A의 한 증상**이다.
- A를 고칠 때 걸림 — 글자 영역 표기가 백엔드 `w/h` vs AI `width/height`.
- ⚠️ STATUS·CLAUDE.md의 **"스텁 대신 `analyze_stroke_order_by_position`이 대신 동작한다"** 는
  **AI 패키지 안에서만 참**이고 통합본에서는 거짓이다.
- **A·B·C·E·F는 사실상 한 뿌리**다 — `ai/canvas/canvas_quality_analyzer.py`의
  `analyze_canvas_writing()`이 이미 다 계산해서 반환하지만(획순 위치·모양 비교, 필압·속도
  프로필, 교정 플래그, 자간 40%, 크기 상대 기준 전부 포함) `backend/app/services/ai_adapters.py`가
  이 함수를 아예 import하지 않는다. `analyze-detail` 라우트를 이 함수 호출로 바꾸면 A·C·E·F가
  한 번에 딸려 온다. B는 독립적으로도 뺄 수 있지만(`_stroke_speed_stats`는 `target_text` 불필요)
  현재 private 헬퍼라 어댑터에 노출하는 작업이 별도로 필요하다. 2026-08-09 재확인: 여전히 미해결
  — target_text 미전달(§4-3), `w/h` vs `width/height` 키 불일치, 응답 스키마·DB 컬럼 추가가
  걸려 있어 §5의 8·10번 같은 "몇 줄 연결"이 아니라 별도 설계가 필요한 작업이다.

---

## 참고 — 확인된 사실

- **전처리 리사이즈는 속도와 무관하다.** 탐지기가 글자 높이 ~90px에 맞춰 입력을 다시 스케일하므로
  배율이 상쇄된다. 다운스케일해도 추론 시간은 안 줄고 획만 깨진다.
- **획순 표준은 문헌이 아니라 한글 조합 규칙(유니코드 산술)으로 생성**했다. 문헌 근거는
  복수 정본(ㅌ 등) 쪽이다.
- **캔버스 LSTM 스텁 2곳은 §4의 3번과 별개 층위**다 — 글자를 알려줘도 스텁은 그대로 남는다.
