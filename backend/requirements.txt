fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.30.0
psycopg2-binary==2.9.10
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
firebase-admin==6.5.0
python-multipart==0.0.9
greenlet==3.1.1
redis==5.0.8
boto3==1.35.36

# ── AI 파트 연동 (ai/ai_adapters.py 실구현이 사용) — ai/requirements.txt와 버전 통일 ──
# 자세한 설치 순서·주의사항은 ai/BACKEND_INTEGRATION.md §3, ai/requirements.txt 참고
--extra-index-url https://download.pytorch.org/whl/cpu
opencv-python-headless==4.11.0.86    # ⚠️ 5.x 금지 (craft_detector.py가 4.x API 가정)
numpy==2.5.1
torch==2.13.0+cpu
torchvision==0.28.0+cpu
scipy==1.18.0
gdown==6.1.0
Pillow==12.3.0
# craft-text-detector==0.4.3는 여기 없음 — 잘못된 opencv 핀 때문에 반드시 별도로
# --no-deps 설치 + vgg16_bn.py 1줄 패치 필요 (ai/requirements.txt 하단 절차 그대로 따를 것)