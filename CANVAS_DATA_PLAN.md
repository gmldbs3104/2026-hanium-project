# 캔버스 모드 AI 학습 데이터 — 팀 논의 필요사항

> 작성일: 2026-07-09 (AI 트랙)
> 요약: 캔버스 모드(손글씨 획순/자간/크기 분석)의 AI 모델을 학습시키려면 실제 사용자
> 필기 데이터가 필요한데, 지금은 없습니다. 어떻게 모을지 팀 논의가 필요합니다.

---

## 1. 무슨 데이터가 왜 필요한가

캔버스 모드는 사용자가 화면에 손가락/펜으로 쓴 획(점들의 좌표+시간+압력 시퀀스)을
받아서 두 가지를 판단합니다.

1. **어느 획들이 한 글자에 속하는지** (획 그룹핑)
2. **획순이 표준과 맞는지, 자간·크기는 적절한지** (획순/자간/크기 분석)

이 두 가지를 정확히 하려면 AI 모델(LSTM)이 필요하고, 모델을 학습시키려면
**실제 사람이 캔버스에 쓴 획 데이터**가 있어야 합니다.

## 2. 왜 표준 폰트나 정답만으로는 안 되는가

"바른 글씨체가 이미 정해져 있으니 그 기준(폰트)으로 학습하면 되지 않냐"는 질문이
나왔는데, 정답은 절반만 맞습니다.

- **"정답이 뭔지"는 이미 다 알고 있습니다.** 한글 획순은 초성·중성·종성 자모 단위로
  이미 표준이 정해져 있어서, 데이터 수집 없이 코드로 만들었습니다
  (11,172자 전체 획순 자동 계산 완료).
- **문제는 "사용자가 실제로 그린 삐뚤빼뚤한 획을 보고 뭘 쓰려고 했는지 해석하는 것"**
  입니다. 이건 정답(폰트)에는 없는 정보입니다 — 사람마다 손이 떨리는 정도, 쓰는
  속도, 삐뚤어지는 정도가 다 다른데, 이런 "실제 시도의 다양성"을 모델이 배우려면
  실제 사람이 쓴 데이터가 필요합니다.

비유하자면, 이미지 모드에서 쓰고 있는 CRAFT 모델도 비슷한 문제를 겪었습니다 —
깨끗한 데이터로만 학습된 모델은 실제 카메라로 찍은 지저분한 손글씨 사진을 잘
못 찾았고, 그래서 AI Hub의 실제 손글씨 사진으로 다시 학습시켜야 했습니다. 캔버스
모드도 똑같은 이유로 실제 사용자 데이터가 필요합니다.

## 3. AI Hub 같은 기존 데이터셋이 있는지 찾아봤는데 — 없습니다

이미지 모드 파인튜닝에 쓴 AI Hub 053 데이터셋은 "사진(정적 이미지)" 데이터입니다.
캔버스 모드에 필요한 건 "펜이 움직인 좌표+시간 기록(온라인 필기 데이터)"인데, 이건
성격이 다릅니다. 검색해본 결과:

- AI Hub의 손글씨 관련 데이터셋들은 전부 사진/이미지 기반이었습니다 (온라인 필기
  데이터 없음).
- 중국어(CASIA-OLHWDB), 베트남어(VNOnDB), 일본어 쪽엔 이런 온라인 필기 데이터셋이
  있는데, 한국어는 이에 대응하는 공개 데이터셋을 찾지 못했습니다.
- 1990년대 학술용 데이터베이스(PE92 등)가 있긴 하지만 오래됐고 접근 경로가
  불확실합니다.

**즉 "AI Hub에서 하나 더 찾으면 되지 않을까"는 이번엔 안 통합니다.** 직접 확보하는
방법을 고민해야 합니다.

## 4. 제안하는 방향 — 2단계 학습

이미지 모드 파인튜닝(craft_mlt_25k 사전학습 → AI Hub 053 파인튜닝) 때와 같은 방식을
씁니다.

```
1단계: 합성 데이터로 pretrain          ← 지금 구현 완료, 데이터 수집 불필요
       (한글 자모 획 모양 + 배치 규칙을 코드로 만들고,
        사람처럼 손떨림/속도변화 노이즈를 섞어서 대량 생성)

2단계: 실제 사용자 데이터로 fine-tune   ← 팀 논의 필요, 아래 5번 참고
```

1단계는 데이터가 전혀 없어도 지금 바로 진행 가능해서 이미 만들어뒀습니다
(`ai/canvas/synthetic_stroke_generator.py`). 다만 이것만으로는 "실제 사람의 삐뚤빼뚤한
손글씨"에 대한 정확도가 한계가 있어서, 2단계가 결국 필요합니다.

## 5. 2단계(실제 데이터) 확보 방법 — 논의가 필요한 부분

### 5.1 최선의 방법 — "연습" 기능 자체가 곧 데이터 수집 (권장)

수행계획서에 이미 있는 "손글씨 연습"(자음/모음/받침 단계별 쓰기), "문장 쓰기" 기능을
그대로 쓰면 됩니다. 핵심 아이디어: **앱이 사용자에게 "이 글자를 써보세요: 가" 하고
목표를 먼저 제시하고 따라 쓰게 하는 구조라, 사용자가 쓴 획 데이터에 정답(목표 글자)이
이미 붙어서 나옵니다.**

- 별도로 "이 획들이 무슨 글자였는지" 라벨링할 필요가 없음 — 앱이 제시한 목표 = 정답
- 한 글자씩 연습하는 단계라면 SFR-004C(획 그룹핑) 정답도 공짜로 따라옴 (그 화면에
  쓴 획은 전부 그 한 글자에 속함이 확정)
- 문장 쓰기 단계도 목표 문장을 알고 있으니, 글자 수만큼 칸을 나눠 입력받는 워크시트
  형태로 만들면 그룹핑 정답까지 자동 확보 가능
- 새 기능을 따로 안 만들어도 됨 — **원래 만들려던 연습 기능이 곧 데이터 수집기**가
  되는 구조라, 앱이 배포되고 사용자가 연습 기능을 쓰기 시작하는 순간부터 정상적인
  서비스 이용 자체가 라벨링된 학습 데이터 축적으로 이어짐
- 배포 전이라도 팀원들이 연습 화면으로 직접 테스트하는 것만으로 초기 데이터 확보 가능

이 방식이면 프론트엔드 쪽에서 추가로 필요한 건 "제시한 목표 글자/문장 + 사용자가 쓴
획 데이터"를 세트로 서버에 전송하는 것 정도라, 개발 부담도 크지 않습니다.

### 5.2 보조 방법 (필요 시)

- **팀 내부 + 지인 대상 소규모 파일럿 수집**: 배포 전 초기 모델 검증용으로 팀원/지인
  몇 명에게 연습 화면을 미리 써보게 하는 정도.
- **크라우드소싱 설문**: 자유 문장(연습 목표에 없는 문장) 스타일까지 다양하게 모으고
  싶을 때 보충용으로 고려.

**논의가 필요한 것들**:
- "제시한 목표 + 획 데이터"를 서버로 보내는 시점/엔드포인트를 언제 만들지
- 목표 규모(몇 명, 몇 글자)
- 개인정보/동의 처리 방식 (SFR-001의 개인정보 처리 원칙과 동일하게 맞춰야 함 —
  "연습 데이터를 모델 개선에 활용" 동의를 어디서 받을지)
- 수집 주체/역할 분담

## 6. 지금 당장은 문제없습니다

1단계(합성 데이터 학습)까지는 실 데이터 없이 진행 가능하니, 2단계 계획은 급하게
정하지 않아도 됩니다. 다만 시간이 걸리는 일(사람 모으기, 동의 절차 등)이라 미리
방향만 잡아두면 좋을 것 같습니다.
